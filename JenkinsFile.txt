pipeline {
    agent any

    tools {
        maven "Maven-3.9"
    }

    environment {
        JAVA_HOME = 'C:\\Program Files\\Java\\jdk-26'
        DEPLOY_PATH = 'C:\\applications\\logins'
        APP_NAME = 'app'
    }

    stages {

        stage('Checkout Code') {
            steps {
                checkout scmGit(
                    branches: [[name: '*/main']],
                    userRemoteConfigs: [[url: 'https://github.com/naveenkumarr7979/COMParent.git']]
                )
            }
        }

        stage('Build with Maven') {
            steps {
                   bat 'mvn clean install -DskipTests'
            }
        }
}
   
}